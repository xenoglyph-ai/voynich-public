# LaTeX build artifacts — regenerated by the Makefile
build/
_stage/

# The final bundle IS tracked — it's the deliverable uploaded to
# arXiv, and having it committed lets the mobile reviewer (Jake) grab
# it directly from GitHub rather than rebuilding. Regenerated by
# `make package`; bit-for-bit reproducible given identical source.
# Track it explicitly:
!voynich_arxiv_submission.tar.gz

# Editor + OS cruft
*.swp
*.swo
.DS_Store
._*
